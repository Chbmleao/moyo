[build]
builder = "nixpacks"
buildCommand = "npm install && npx turbo run build --filter=@moyo/frontend"

[deploy]
startCommand = "npm run start --workspace=@moyo/frontend"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
